import {describe,expect,it}from"vitest";import{runJavaTask}from"@harness/java-test";const TEST_MAIN="import java.util.*;import java.util.concurrent.*;public class TestMain{public static void main(String[]args)throws Exception{Map<String,Integer>attempts=new ConcurrentHashMap<>();var result=Solution.process(List.of(\"a\",\"b\"),value->{int n=attempts.merge(value,1,Integer::sum);if(value.equals(\"a\")&&n==1)throw new Exception(\"retry\");return value.toUpperCase();});if(!result.values().equals(List.of(\"A\",\"B\"))||result.retries()!=1)throw new AssertionError(result);System.out.print(\"OK\");}}\n";describe("Moduł UTP: współbieczny procesor",()=>{it("spełnia kontrakt UTP",async()=>{await expect(runJavaTask(import.meta.dirname,TEST_MAIN)).resolves.toBe("OK");});});
