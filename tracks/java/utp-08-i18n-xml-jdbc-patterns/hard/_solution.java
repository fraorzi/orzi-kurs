import java.text.*;import java.util.*;class Solution{static String money(long cents,Locale locale){NumberFormat format=NumberFormat.getCurrencyInstance(locale);return format.format(cents/100.0);}static String insertSql(){return "INSERT INTO imports (external_id, amount) VALUES (?, ?)";}static boolean safeXml(String xml){return !xml.contains("<!DOCTYPE")&&!xml.contains("<!ENTITY");}}
