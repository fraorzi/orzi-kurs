import{describe,expect,it}from"vitest";import{runJavaTask}from"@harness/java-test";const TEST_MAIN="import java.util.*;public class TestMain{public static void main(String[]args){List<String>events=new ArrayList<>();Set<String>done=new HashSet<>();Solution.Store store=new Solution.Store(){public boolean done(String id){return done.contains(id);}public void effect(String payload){events.add(payload);}public void mark(String id){done.add(id);}};if(!Solution.process(\"e1\",\"x\",store)||Solution.process(\"e1\",\"x\",store))throw new AssertionError();if(!events.equals(List.of(\"x\")))throw new AssertionError();System.out.print(\"OK\");}}\n";describe("Moduł TPO: idempotentny worker",()=>{it("spełnia kontrakt SKJ/TPO",async()=>{await expect(runJavaTask(import.meta.dirname,TEST_MAIN)).resolves.toBe("OK");});});
