# Zaplanuj migrację na JDK 25

Zwróć katalog najważniejszych feature’ów z poprawnym statusem `STABLE` albo
`PREVIEW`. `core()` ma zawierać wyłącznie stabilne elementy, bez wymagania
`--enable-preview`.
