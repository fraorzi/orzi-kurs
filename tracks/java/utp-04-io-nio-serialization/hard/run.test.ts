import {describe,expect,it}from"vitest";import{runJavaTask}from"@harness/java-test";const TEST_MAIN="import java.nio.file.*;import java.util.*;public class TestMain{public static void main(String[]args)throws Exception{Path dir=Files.createTempDirectory(\"utp\");try{Files.createDirectories(dir.resolve(\"x\"));Files.writeString(dir.resolve(\"x/a.txt\"),\"a\");if(Solution.files(dir).size()!=1)throw new AssertionError();Solution.save(dir.resolve(\"out.txt\"),List.of(\"ą\",\"b\"));if(!Files.readAllLines(dir.resolve(\"out.txt\")).equals(List.of(\"ą\",\"b\")))throw new AssertionError();}finally{try(var s=Files.walk(dir)){s.sorted(Comparator.reverseOrder()).forEach(p->{try{Files.delete(p);}catch(Exception ignored){}});}}System.out.print(\"OK\");}}\n";describe("java.io, NIO.2 i bezpieczna serializacja",()=>{it("spełnia kontrakt UTP",async()=>{await expect(runJavaTask(import.meta.dirname,TEST_MAIN)).resolves.toBe("OK");});});
