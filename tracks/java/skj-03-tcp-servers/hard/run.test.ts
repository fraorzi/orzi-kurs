import{describe,expect,it}from"vitest";import{runJavaTask}from"@harness/java-test";const TEST_MAIN="public class TestMain{public static void main(String[]args){var sessions=new Solution.Sessions(2);if(!sessions.tryOpen()||!sessions.tryOpen()||sessions.tryOpen())throw new AssertionError();sessions.close();if(!sessions.tryOpen())throw new AssertionError();System.out.print(\"OK\");}}\n";describe("TCP, wielu klientów i shutdown",()=>{it("spełnia kontrakt SKJ/TPO",async()=>{await expect(runJavaTask(import.meta.dirname,TEST_MAIN)).resolves.toBe("OK");});});
