# Suma parzystych (track wewnętrzny)

> Track `_smoke` jest wewnętrzny — służy wyłącznie do samotestu harnessu.
> Prefiks `_` sprawia, że jest ukryty w katalogu UI i pomijany przez auto-commit.

Zagadnienie testowe: przefiltruj liczby parzyste i zsumuj je bez mutowania wejścia.
