import { sum } from "./math.js";

export function average(nums) {
  // TODO: użyj `sum(nums)` i podziel przez liczbę elementów.
  // Pamiętaj o pustej tablicy (zwróć 0).
  throw new Error("not implemented");
}
