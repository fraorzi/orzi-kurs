# Odczytaj asynchroniczny slug

Tryb: od zera. Napisz rozwiązanie w `starter.ts`, korzystając z podanych sygnatur i typów.

Uzupełnij `readProductSlug`: zaczekaj na `params`, pobierz `slug`, usuń białe znaki
z brzegów i odrzuć pustą wartość błędem `Nieprawidłowy slug produktu`.
