# Popraw zdalny obraz produktu

Skonfiguruj `remotePatterns` wyłącznie dla HTTPS hosta `cdn.example`, ścieżki
`/products/**`, bez portu i query. W `ProductHero` zastąp `<img>` przez `Image` z
wymiarami 1200×800, `sizes="(max-width: 768px) 100vw, 50vw"` i przekazanym altem.
