# Włącz aktualny model Cache Components

Skonfiguruj aplikację dla Next 16. Ustaw `cacheComponents: true` w obiekcie
`NextConfig`. Nie dodawaj dawnych flag eksperymentalnych PPR, `dynamicIO` ani
`useCache`.
