## Hint 1

Najpierw oblicz `isDynamic`: runtime albo cached z profilem `seconds`.

## Hint 2

Jeżeli węzeł nie jest dynamiczny, dodaj jego nazwę do `staticShell` niezależnie od
pola `async`.

## Hint 3

Dla dynamicznego węzła wybierz `dynamicHoles` albo `blockers` na podstawie
`insideSuspense`.
