## Hint 1

Zaimportuj `Suspense` z Reacta i owiń wyłącznie `<ViewerGreeting />`.

## Hint 2

Fallback może być paragrafem z `role="status"`.

## Hint 3

Komponentów Reacta nie wywołuj jak zwykłych funkcji. To `ViewerGreeting` ma pozostać
`async` i wykonać własne `await`.
