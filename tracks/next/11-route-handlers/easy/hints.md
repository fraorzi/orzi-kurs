## Hint 1

Utwórz `new URL(request.url)` i odczytaj `searchParams`.

## Hint 2

Po `Number` sprawdź `Number.isInteger(value) && value > 0`.

## Hint 3

`Response.json(body, { status, headers })` ustawia cały kontrakt odpowiedzi.
