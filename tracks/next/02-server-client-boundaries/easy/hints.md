## Hint 1

`"use client"` powinno być pierwszą instrukcją tylko w `FavoriteButton.tsx`.

## Hint 2

`ProductCard` importuje gotową wyspę i przekazuje jej boolean, nie setter ani callback.

## Hint 3

Stan przycisku pozostaje lokalny; reszta karty nie potrzebuje hydracji.
