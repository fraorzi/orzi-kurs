## Hint 1

Root layout jest jedynym layoutem, który musi zawierać elementy `html` i `body`.

## Hint 2

Link pomijający nawigację powinien wskazywać `href="#main-content"`, a `main`
potrzebuje odpowiadającego `id`.

## Hint 3

Typ `children` to `ReactNode`, importowany jako typ z `react`.
