## Hint 1

`error.tsx` zaczyna się od `"use client"` i importuje `useEffect`.

## Hint 2

Efekt zależy od `[error]`; przycisk może dostać bezpośrednio `onClick={unstable_retry}`.

## Hint 3

W not-found zaimportuj `Link` z `next/link`, zamiast używać zwykłego `<a>`.
