## Hint 1

Helper `first` może zwracać `Array.isArray(value) ? value[0] : value`.

## Hint 2

Poprawna page spełnia `Number.isInteger(page) && page > 0`; potem użyj `Math.min`.

## Hint 3

Jeden helper `hrefFor(page)` może budować `URLSearchParams` dla obu kierunków.
