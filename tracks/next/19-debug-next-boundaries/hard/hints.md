## Hint 1

Komponent `Activity` może sam wykonać `const items = await getActivity()` i zwrócić listę.

## Hint 2

Po refaktorze `Page()` zwraca React element, nie Promise; Suspense otacza wywołanie `<Activity />`.
