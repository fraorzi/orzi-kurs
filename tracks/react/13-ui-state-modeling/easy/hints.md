## Hint 1

Każdy wariant unii ma literalne pole `status`.

## Hint 2

Tylko wariant `success` zawiera `user`, a tylko `error` zawiera `message`.

## Hint 3

Switch po `state.status` automatycznie zawęzi dostępne pola.
