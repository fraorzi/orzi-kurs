## Hint 1

Utwórz context poza komponentami z domyślnym `"pl"`.

## Hint 2

W React 19 sam `LocaleContext` jest komponentem providera i przyjmuje prop `value`.

## Hint 3

`Greeting` wywołuje `useContext(LocaleContext)` i wybiera tekst na podstawie wyniku.
