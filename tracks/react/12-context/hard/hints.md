## Hint 1

`dispatch` z `useReducer` ma stabilne identity.

## Hint 2

Utwórz osobny `CounterStateContext` dla liczby i `CounterDispatchContext` dla
dispatchu.

## Hint 3

`CounterValue` czyta tylko pierwszy context, a `IncrementButton` tylko drugi.
