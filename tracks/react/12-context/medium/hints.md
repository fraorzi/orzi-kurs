## Hint 1

Typ contextu to `Session | null`, a wartość początkowa `createContext` to `null`.

## Hint 2

Całe sprawdzenie `null` umieść w `useSession`, aby konsumenci dostawali już typ
`Session`.

## Hint 3

Provider renderuj jako `<SessionContext value={session}>`.
