## Hint 1

Callback ref dostaje węzeł DOM po podpięciu.

## Hint 2

Jeśli `node` istnieje, zwróć bezpośrednio wynik `registry.attach(field.id, node)`.

## Hint 3

React 19 potraktuje zwróconą funkcję jako cleanup danego refa.
