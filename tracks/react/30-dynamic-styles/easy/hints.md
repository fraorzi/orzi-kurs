## Hint 1

Najpierw ogranicz `value` do przedziału od `0` do `max`.

## Hint 2

Procent wymaga jawnej jednostki: `width: `${percent}%``.

## Hint 3

`style` w React przyjmuje obiekt, nie tekst znany z atrybutu HTML.
