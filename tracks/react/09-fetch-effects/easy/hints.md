## Hint 1

Wynik stanu zapisz razem z `userId`, dla którego został pobrany.

## Hint 2

Jeśli zapisany wynik ma inne `userId` niż bieżący prop, renderuj stan ładowania.

## Hint 3

W efekcie obsłuż zarówno `.then`, jak i `.catch`, a zależności ustaw na `loadUser`
i `userId`.
