## Hint 1

Klucz może wyglądać jak `["projects", { status }]`.

## Hint 2

`staleTime` podaje się w milisekundach.

## Hint 3

`queryFn` otrzymuje obiekt z `signal`; nie twórz własnego kontrolera.

## Hint 4

`isPending` oznacza brak danych, a `isFetching` może towarzyszyć już widocznej liście.
