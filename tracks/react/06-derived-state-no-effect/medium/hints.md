## Hint 1

`products` i `query` są pełnym zestawem danych potrzebnym do obliczenia wyniku.

## Hint 2

Przed `includes` sprowadź obie strony do małych liter.

## Hint 3

Usuń stan `visibleProducts`; lokalna stała z `filter` przeliczy się przy każdej
zmianie propsów lub query.
