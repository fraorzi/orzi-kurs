## Hint 1

Zaimportuj `createPortal` z `react-dom`.

## Hint 2

Zwróć `createPortal(<p role="status">...</p>, container)`.

## Hint 3

Kontener jest już gotowym `HTMLElement`; komponent nie musi go tworzyć.
