## Hint 1

Najpierw obsłuż early return dla `showDetails === false`.

## Hint 2

Po early return wywołaj `const theme = use(ThemeContext)`.

## Hint 3

`use` importuje się z `react`; w przeciwieństwie do zwykłych Hooków może być
wywołane po warunku.
