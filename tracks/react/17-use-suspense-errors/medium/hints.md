## Hint 1

Utwórz komponent `UserDetails`, który przyjmuje Promise w propsie.

## Hint 2

W `UserDetails` wywołaj `const user = use(userPromise)`.

## Hint 3

`UserDetails` musi być potomkiem `<Suspense fallback={...}>`.
