## Hint 1

Zadeklaruj jeden `useState` w `SyncedFields`.

## Hint 2

Przekaż tę samą wartość i setter do dwóch instancji pola.

## Hint 3

Komponent potomny renderuje `value={value}` i wywołuje `onChange(nextValue)`.
