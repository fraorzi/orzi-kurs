## Hint 1

Stan kontrolera to `selectedId: string | null`, nie indeks.

## Hint 2

Mapując elementy, porównaj `selectedId === getId(item)`.

## Hint 3

Wywołaj render prop z handlerem ustawiającym ID konkretnego elementu.
