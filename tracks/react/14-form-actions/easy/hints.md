## Hint 1

Funkcja przekazana do `action` otrzymuje jeden argument `FormData`.

## Hint 2

Odczytaj `formData.get("query")`, zamień wynik na string i wywołaj `trim()`.

## Hint 3

Nie ustawiaj `value` inputa. React resetuje niekontrolowane pola po pomyślnym
zakończeniu funkcji `action`.
