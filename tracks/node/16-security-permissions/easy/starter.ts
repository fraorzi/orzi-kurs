export function solve(
  entry: string,
  access: {
    read?: readonly string[];
    write?: readonly string[];
    worker?: boolean;
    child?: boolean;
  },
): string[] {
  throw new Error("TODO");
}
