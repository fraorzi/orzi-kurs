# Rozpoznaj format modułu

Napisz resolver formatu dla `.mjs`, `.cjs`, `.mts`, `.cts` i zwykłych plików zależnych od pola `type`.

Kod ma pozostać TypeScript-first, deterministyczny i możliwy do testowania bez zewnętrznych usług.
