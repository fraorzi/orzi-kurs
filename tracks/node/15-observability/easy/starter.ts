export function solve(
  input: {
    level: string;
    message: string;
    requestId?: string;
    fields?: Record<string, unknown>;
  },
  now: () => string,
): string {
  throw new Error("TODO");
}
