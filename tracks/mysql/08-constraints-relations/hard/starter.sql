CREATE TABLE projects (id BIGINT PRIMARY KEY, tenant_id BIGINT NOT NULL, slug VARCHAR(80) NOT NULL UNIQUE); CREATE TABLE tasks (id BIGINT PRIMARY KEY, tenant_id BIGINT NOT NULL, project_id BIGINT NOT NULL, FOREIGN KEY (project_id) REFERENCES projects(id));
