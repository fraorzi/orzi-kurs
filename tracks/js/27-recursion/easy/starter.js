export function factorial(n) {
  // TODO: baza n <= 1 -> 1; krok n * factorial(n - 1)
}

export function pow(base, exp) {
  // TODO: baza exp === 0 -> 1; krok base * pow(base, exp - 1); bez ** i Math.pow
}
