export function sumNested(value) {
  // TODO: jeśli value to tablica — zsumuj rekurencyjnie elementy; jeśli liczba — zwróć ją
}

export function treeSum(node) {
  // TODO: value węzła + suma treeSum dzieci (children może być puste/pominięte)
}
