export function debounce(fn, delay, options = {}) {
  // TODO:
  //  - trailing (domyślnie): odpal po ciszy z ostatnimi argumentami
  //  - leading: odpal na starcie serii, potem tłum do ciszy (bez trailing)
  //  - dołącz metodę .cancel() czyszczącą zaplanowany timer
}
