export function createQueue() {
  // TODO: bufor wartości + bufor czekających konsumentów (deferredów).
  //       push: jeśli ktoś czeka — rozstrzygnij go; inaczej dołóż wartość do bufora.
  //       pull: jeśli jest wartość — zwróć ją; inaczej utwórz deferred i zwróć jego promisę.
}
