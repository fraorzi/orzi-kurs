export function createEmitter() {
  // TODO: { on, once, emit, off }
  //  - on zwraca funkcję unsubscribe
  //  - once wypisuje słuchacza po pierwszym wywołaniu
  //  - emit przekazuje wszystkie argumenty (...args)
}
