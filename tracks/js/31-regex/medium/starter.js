export function capitalizeWords(str) {
  // TODO: replace(/\b\w/g, litera => litera.toUpperCase())
}

export function censor(text, word) {
  // TODO: replace(new RegExp(word, "gi"), "*".repeat(word.length))
}
