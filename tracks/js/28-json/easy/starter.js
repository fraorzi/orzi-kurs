export function stringifyFields(obj, fields) {
  // TODO: JSON.stringify z replacer-tablicą fields i wcięciem 2
}

export function safeParse(str, fallback = null) {
  // TODO: try JSON.parse; przy błędzie zwróć fallback
}
