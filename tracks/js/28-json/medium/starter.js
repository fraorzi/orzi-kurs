export function stringifyHidingSecrets(obj) {
  // TODO: JSON.stringify z replacerem-funkcją; zwróć undefined dla "password" i "token"
}

export function parseWithDates(str) {
  // TODO: JSON.parse z reviverem; string ISO daty -> new Date(value)
}
