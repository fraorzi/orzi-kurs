export function approxEqual(a, b, tolerance = 1e-9) {
  // TODO: Math.abs(a - b) < tolerance
}

export function toFixedNumber(value, digits) {
  // TODO: zaokrąglij do digits miejsc, ale zwróć number (Number(value.toFixed(digits)))
}
