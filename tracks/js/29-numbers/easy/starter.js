export function roundTo(value, decimals) {
  // TODO: zaokrąglij do decimals miejsc, zwróć liczbę (Math.round(value * 10**decimals) / 10**decimals)
}

export function isInteger(value) {
  // TODO: Number.isInteger(value)
}
