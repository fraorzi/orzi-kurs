export function parseMoney(str) {
  // TODO: rozdziel na złote i grosze po ".", uzupełnij grosze do 2 cyfr,
  //       policz na liczbach całkowitych (bez parseFloat * 100)
}

export function formatMoney(cents) {
  // TODO: złote = floor(cents/100), grosze = cents % 100 wyrównane do 2 cyfr
}
