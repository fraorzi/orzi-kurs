export function createWeakValueMap() {
  // TODO: Map trzymająca słabe referencje do wartości + rejestr finalizacji,
  //       który po zebraniu obiektu usuwa jego wpis (held value = klucz).
  //       set(key, value): zapisz słabą referencję i zarejestruj wartość w rejestrze.
  //       get(key): odczytaj przez deref; has(key): czy odczyt dał wartość.
}
