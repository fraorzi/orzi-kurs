export function intersection(a, b) {
  // TODO: elementy z a obecne też w b, bez duplikatów; użyj Set(b) dla O(1) has
}

export function union(a, b) {
  // TODO: wszystkie elementy z a i b, bez duplikatów
}

export function difference(a, b) {
  // TODO: elementy z a, których nie ma w b; użyj Set(b)
}
