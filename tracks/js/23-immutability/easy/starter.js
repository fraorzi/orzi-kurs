export function updateField(obj, key, value) {
  // TODO: zwróć nowy obiekt (spread) z nadpisanym key; nie mutuj obj
}

export function addItem(arr, item) {
  // TODO: zwróć nową tablicę z dopisanym item; nie używaj push
}
