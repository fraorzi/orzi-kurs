export function makeReadLog() {
  // TODO: WeakMap message -> date; zwróć { markRead(message, date), readAt(message) }
}

// TODO: module-owy WeakMap na prywatne salda

export class Account {
  // TODO: constructor(initial) zapisuje saldo w WeakMap (klucz this),
  //       deposit(amount) je zwiększa, getter balance je zwraca
}
