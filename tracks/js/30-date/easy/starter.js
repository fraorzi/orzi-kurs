const DAY = 24 * 60 * 60 * 1000;

export function addDays(date, days) {
  // TODO: zwróć nowy Date przesunięty o days dni (getTime + days * DAY)
}

export function daysBetween(a, b) {
  // TODO: (b - a) w dniach, zaokrąglone
}
