export function observable(target, onChange) {
  // TODO: dla każdego klucza target zdefiniuj na nowym obiekcie akcesor get/set
  //       (Object.defineProperty, enumerable: true). Setter woła onChange tylko
  //       przy zmianie wartości. Stan trzymaj w wewnętrznym obiekcie (kopia target).
}
