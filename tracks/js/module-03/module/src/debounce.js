export function debounce(fn, waitMs) {
  // TODO: zwróć funkcję, która odracza wywołanie `fn` o `waitMs`.
  // Każde kolejne wywołanie w oknie czasu kasuje poprzedni timer i ustawia nowy,
  // więc `fn` odpala się raz — z ostatnimi argumentami — po ciszy trwającej waitMs.
  // Trzymaj uchwyt timera w domknięciu (clearTimeout + setTimeout).
}
