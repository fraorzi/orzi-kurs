export function naturals() {
  // TODO: nieskończony iterable wydający 1, 2, 3, ... (bez done: true)
}

export function take(iterable, n) {
  // TODO: pobierz leniwie pierwsze n elementów (iterable[Symbol.iterator]().next())
}
