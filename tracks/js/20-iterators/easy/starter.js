export function range(start, end) {
  // TODO: zwróć obiekt z [Symbol.iterator]() zwracającym iterator (next -> {value, done})
}

export function toArray(iterable) {
  // TODO: zbierz elementy iterable do tablicy przez for..of
}
