## Hint 1

`cache[id]` ma typ `User | undefined`; użyj `?? null`.

## Hint 2

W pętli przypisz odczyt do lokalnej `const`, aby zawężenie nie wymagało ponownego
indeksowania.

## Hint 3

Pusta lista naturalnie przejdzie pętlę bez trafienia i zwróci `null`.
