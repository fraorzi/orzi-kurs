## Hint 1

Sygnatura ma przyjąć `parse: (value: unknown) => T`.

## Hint 2

Oddziel `try/catch` dla `JSON.parse` od `try/catch` parsera domenowego.

## Hint 3

Przypisz wynik `JSON.parse` do lokalnego `value: unknown`.
