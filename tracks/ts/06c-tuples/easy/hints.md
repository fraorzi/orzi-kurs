## Hint 1

`Range` to `readonly [start: number, end: number]`.

## Hint 2

Wynik jest unią `[ok: true, range: Range] | [ok: false, message: string]`.

## Hint 3

Po `split("..")` sprawdź długość, `Number.isInteger` i kolejność końców.
