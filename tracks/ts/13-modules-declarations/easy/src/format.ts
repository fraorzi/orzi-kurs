import type { Money } from "./types";

export function minorUnits(money: Money): number {
  // TODO: zaokrąglona liczba groszy/centów
  return 0;
}

export function formatMoney(money: Money): string {
  // TODO: "12.50 PLN"
  return "";
}
