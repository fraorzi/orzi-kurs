## Hint 1

`formatMoney` korzysta z `toFixed(2)`, a `minorUnits` z `Math.round(amount * 100)`.

## Hint 2

Wartości re-eksportuj zwykłym `export { ... }`.

## Hint 3

Typy re-eksportuj przez `export type { Currency, Money }`.
